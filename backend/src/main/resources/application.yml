server:
  port: 8080
  address: 0.0.0.0

spring:
  servlet:
    multipart:
      max-file-size: 10GB
      max-request-size: 10GB
  threads:
    virtual:
      enabled: true

packetdrop:
  upload-dir: uploads
  history-file: data/history.json
  app-name: PacketDrop
